{
  "private": true,
  "version": "1.0.1",
  "name": "@snowpack/test-plugin-resolve-compound",
  "description": "A test to make sure that plugins can output compound file extensions",
  "scripts": {
    "start": "snowpack dev",
    "testbuild": "snowpack build"
  },
  "devDependencies": {
    "js-string-escape": "^1.0.1",
    "snowpack": "^3.0.0"
  }
}
